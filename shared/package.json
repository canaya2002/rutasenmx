{
  "name": "@rutasmx/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types + API contract between web (Next.js) and mobile (Expo). No build step — consumed as source via tsconfig paths.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false
}
